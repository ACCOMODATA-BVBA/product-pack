<odoo>
    <template id="website_event_sale.registration_template" inherit_id="website_event.registration_template">
        <xpath expr="//div[hasclass('o_wevent_registration_multi_select')]" position="inside">
            <t t-if="ticket.total_pack_price">
                <span t-field="ticket.total_pack_price"
                      t-options="{'widget': 'monetary', 'from_currency': event.company_id.sudo().currency_id, 'display_currency': website.pricelist_id.currency_id}"/>
                <span itemprop="price" class="d-none" t-esc="ticket.price"/>
                <span itemprop="priceCurrency" class="d-none" t-esc="website.pricelist_id.currency_id.name"/>
            </t>
            <span t-else="" class="font-weight-bold text-uppercase">Free</span>
        </xpath>
        <xpath expr="//div[hasclass('o_wevent_price_range')]" position="inside">
            <t t-if="event.event_ticket_ids[-1].total_pack_price > 0">
                <span class="text-dark">
                    From
                    <span t-esc="event.event_ticket_ids[0].total_pack_price" t-options="{'widget': 'monetary', 'from_currency': event.company_id.sudo().currency_id, 'display_currency': website.pricelist_id.currency_id}"/>
                    <t t-if="event.event_ticket_ids[-1].total_pack_price != event.event_ticket_ids[0].total_pack_price">
                        to
                        <span t-esc="event.event_ticket_ids[-1].total_pack_price" t-options="{'widget': 'monetary', 'from_currency': event.company_id.sudo().currency_id, 'display_currency': website.pricelist_id.currency_id}"/>
                    </t>
                </span>
            </t>
        </xpath>
        <!-- Add price information on tickets (mono ticket, aka not in collapse) -->
        <xpath expr="//div[hasclass('o_wevent_registration_single')]//h6" position="after">
            <div class="px-2 text-dark d-flex align-items-center align-self-stretch">
                <t t-if="tickets.total_pack_price">
                    <span t-field="tickets.total_pack_price"
                          t-options="{'widget': 'monetary', 'from_currency': event.company_id.sudo().currency_id, 'display_currency': website.pricelist_id.currency_id}"/>
                    <span itemprop="price" class="d-none" t-esc="tickets.price"/>
                    <span itemprop="priceCurrency" class="d-none" t-esc="website.pricelist_id.currency_id.name"/>
                </t>
                <span t-else="" class="font-weight-bold text-uppercase">Free</span>
            </div>
        </xpath>
    </template>
    <template id="registration_attendee_details" inherit_id="website_event.registration_attendee_details">
        <xpath expr="//div[@class='row']" position="replace">
            <div class="row">
                <div class="col-lg my-2">
                    <label>Name</label>
                    <input class="form-control" type="text" t-attf-name="#{counter}-name" required="This field is required"
                        t-att-value="default_first_attendee.get('name', '') + ' - Ticket #' + str(counter)"/>
                </div>
                <div class="col-lg my-2">
                    <label>Email</label>
                    <input class="form-control" type="email" t-attf-name="#{counter}-email" required="This field is required"
                        t-att-value="default_first_attendee.get('email', '')"/>
                </div>
                <div class="col-lg my-2">
                    <label>Phone <small>(Optional)</small></label>
                    <input class="form-control" type="tel" t-attf-name="#{counter}-phone"
                        t-att-value="default_first_attendee.get('phone', '')"/>
                </div>
                <input class="d-none" type="text" t-attf-name="#{counter}-event_ticket_id" t-attf-value="#{ticket['id']}"/>
            </div>
        </xpath>
    </template>
</odoo>
